# Notion-Turbo
A Chrome/Edge/Firefox/Opera extension that boosts Notion's AI chat performance (faster loading times &amp; reduced CPU/RAM usage) by trimming old chat messages. All previous chat messages are kept on Notion's servers, it just doesn't render them all at once.
